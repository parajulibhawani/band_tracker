<h2>List of all the Venues</h2>
@if(Model.Count == 0)
{
  <p>
    No venues listed yet.
  </p>
}
else
{
  <ul>
    @foreach(var venue in Model)
    {
      <li><a href="/venue/@venue.GetId()">@venue.GetVenueName()</a></li>
    }
  </ul>
}
